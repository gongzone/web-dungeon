import { black, white, gray } from './neutral-colors'
import { primary, primaryHover } from './brand-colors'
import { error, warning, success, info } from './status-colors'

export const colors = {
  black,
  white,
  gray,
  primary,
  primaryHover,
  error,
  warning,
  success,
  info,
}

export type ColorsType = typeof colors
