import type { DefaultTheme } from 'styled-components'

import {
  colors,
  fonts,
  fontSizes,
  letterSpacings,
  lineHeights,
  breakpoints,
} from '../foundation'

export const defaultTheme: DefaultTheme = {
  colors,
  fonts,
  fontSizes,
  letterSpacings,
  lineHeights,
  breakpoints,
}
